import telebot
import datetime

# Замените 'YOUR_TOKEN_HERE' на токен вашего бота от @BotFather
bot = telebot.TeleBot('8058652594:AAHF2FI4zm9T9dvmR4Z2CQ-mbfVRkdHpVSs')

# Словарь с информацией по дням недели (понедельник = 0, воскресенье = 6)
WEEK_INFO = {
    0: "Понедельник:\n"
        "1. Английский язык (каб. 40)\n"
        "2. Биология (каб. 23)\n"
        "3. Физика (каб. 31)\n"
        "4. РОВ (каб. 38)\n"
        "5. Литература (каб. 31)\n"
        "6. Алгебра (каб. 35)\n"
        "7. Физическая культура\n"
        "8. Английский язык (каб. 25)",
    
    1: "Вторник:\n"
        "1. История (каб. 38)\n"
        "2. Химия (каб. 41)\n"
        "3. Геометрия (каб. 35)\n"
        "4. Литература (каб. 27)\n"
        "5. Английский язык (каб. 40/25)\n"
        "6. Вероятность и статистика (каб. 35)\n"
        "7. География (каб. 39)\n"
        "8. ОБЗР (каб. 39)",
    
    2: "Среда:\n"
        "1. Нет занятий\n"
        "2. Обществознание (музей)\n"
        "3. Физика (каб. 31)\n"
        "4. Химия (каб. 41)\n"
        "5. Информатика/Английский язык (каб. 22/40)\n"
        "6. Физическая культура\n"
        "7. Труд (технология, каб. 2)\n"
        "8. История (музей)",
    
    3: "Четверг:\n"
        "1. Физика (каб. 31)\n"
        "2. Биология (каб. 23)\n"
        "3. Информатика/Английский язык (каб. 22/25)\n"
        "4. Русский язык (каб. 24)\n"
        "5. Алгебра (каб. 35)\n"
        "6. «Россия — мои горизонты»",
    
    4: "Пятница:\n"
        "1. География (каб. 39)\n"
        "2. Алгебра (каб. 35)\n"
        "3. Русский язык (каб. 2)\n"
        "4. Геометрия (каб. 35)\n"
        "5. Русский язык (библиотека)\n"
        "6. Литература (библиотека)",
    
    5: "Суббота: время отдыха! Займитесь хобби или встретьтесь с друзьями.",
    6: "Воскресенье: подготовка к новой неделе! Отдохните и настройтесь на понедельник."
}

@bot.message_handler(commands=['start'])
def send_welcome(message):
    bot.reply_to(message, "Привет! Я бот, который рассказывает, что делать в каждый день недели. Отправь /today, чтобы узнать подсказку на сегодня.")


@bot.message_handler(commands=['today'])
def send_today_info(message):
    # Получаем текущий день недели (0 — понедельник, 6 — воскресенье)
    today_weekday = datetime.datetime.now().weekday()
    # Берём информацию из словаря
    info = WEEK_INFO[today_weekday]
    bot.send_message(message.chat.id, info)

@bot.message_handler(commands=['понедельник'])
def send_monday_info(message):
    # Всегда отправляем сообщение, что сегодня понедельник, и расписание
    info = WEEK_INFO[0]  # Расписание на понедельник
    bot.send_message(message.chat.id, info)

@bot.message_handler(commands=['вторник'])
def send_monday_info(message):
    # Всегда отправляем сообщение, что сегодня понедельник, и расписание
    info = WEEK_INFO[1]  # Расписание на понедельник
    bot.send_message(message.chat.id, info)

@bot.message_handler(commands=['среда'])
def send_monday_info(message):
    # Всегда отправляем сообщение, что сегодня понедельник, и расписание
    info = WEEK_INFO[2]  # Расписание на понедельник
    bot.send_message(message.chat.id, info)

@bot.message_handler(commands=['четверг'])
def send_monday_info(message):
    # Всегда отправляем сообщение, что сегодня понедельник, и расписание
    info = WEEK_INFO[3]  # Расписание на понедельник
    bot.send_message(message.chat.id, info)


@bot.message_handler(commands=['пятница'])
def send_monday_info(message):
    # Всегда отправляем сообщение, что сегодня понедельник, и расписание
    info = WEEK_INFO[4]  # Расписание на понедельник
    bot.send_message(message.chat.id, info)

# Запуск бота
if __name__ == '__main__':
    bot.polling(none_stop=True)